
  # Create React UI Components

  This is a code bundle for Create React UI Components. The original project is available at https://www.figma.com/design/lcy2AX4AFfGkkhij6s5cLg/Create-React-UI-Components.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  